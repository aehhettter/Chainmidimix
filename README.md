# Chainmidimix

## APK bauen

Den Devcontainer neu erstellen und anschließend im Terminal ausführen:

```bash
gradle :app:assembleDebug
```

Das Debug-APK liegt danach unter `app/build/outputs/apk/debug/app-debug.apk`.

